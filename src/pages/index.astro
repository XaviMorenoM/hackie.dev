---
// Intentionally empty. Astro i18n (prefixDefaultLocale + redirectToDefaultLocale) refuses to build
// without a root index route; its injected `/` → `/en/` redirect takes priority over this file.
---
